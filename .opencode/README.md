# OpenCode 适配层

这里放 OpenCode 侧的接入说明。

第一次接手当前仓库，先看 `../START-HERE.md`。

## 当前约定

1. OpenCode 接入时仍以 `picasso-dev` 为统一业务入口
2. 方法论增强由 `picasso-dev-methods` 提供
3. OpenCode 端仅维护适配说明，不自行衍生新工作流

## 建议用途

- 会话初始化提示
- 运行边界说明
- 平台特有命令或目录映射说明
